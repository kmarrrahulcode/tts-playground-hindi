# TTS Playground Requirements
# Compatible with Python 3.9 - 3.11
# Note: TTS library does not support Python 3.12+

# Core TTS library
TTS>=0.22.0

# PyTorch (CPU version)
# Note: For Python 3.13, you may need to install from PyTorch nightly or wait for official support
torch>=2.0.0
torchaudio>=2.0.0

# Audio processing
numpy>=1.24.0
soundfile>=0.12.0

# HuggingFace
huggingface-hub>=0.19.0
# TTS 0.22.0 requires transformers < 4.40.0 (BeamSearchScorer was removed in 4.40.0+)
transformers>=4.35.0,<4.40.0

# Note: torchaudio is already included with torch, but explicitly listed for Indri model
# torchaudio is used by both XTTS and Indri models

