# TTS Playground Requirements - Kokoro TTS
# IMPORTANT: Requires Python 3.10-3.12 (NOT compatible with Python 3.13+)
# Use separate virtual environment: venv-kokoro
# Create with: py -3.11 -m venv venv-kokoro

# Kokoro TTS library
kokoro>=0.9.2

# Hindi language support (G2P)
misaki[hi]>=0.9.1

# PyTorch
torch>=2.0.0

# Audio processing
numpy>=1.24.0
soundfile>=0.12.0

# API dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0

# Note: This environment is for Kokoro TTS only
# Use separate virtual environments for other TTS engines
